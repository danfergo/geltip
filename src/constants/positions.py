from math import pi


class Positions:

    @staticmethod
    def start():
        return [0.52, 0, 0.30, pi, 0, pi/2]

    @staticmethod
    def object():
        return [0.52, 0, 0.20, pi, 0, pi/2]
